## 2024-05-21 - Memory vs Reality in Optimization
**Learning:** The memory stated that route-level code splitting was already implemented in `src/App.js` using `React.lazy` and `Suspense`. However, inspection of `src/App.js` revealed static imports.
**Action:** Always verify "known" state or documentation against the actual code before assuming an optimization exists or is needed. Trust the code as the source of truth.
